apiVersion: batch/v1
kind: Job
metadata:
  name: before-a
  namespace: default
spec:
  template:
    spec:
      containers:
        - name: sleep
          image: alpine:latest
          command: ["sleep", "30"]
#          command: [ "false" ]
      restartPolicy: Never
  backoffLimit: 0