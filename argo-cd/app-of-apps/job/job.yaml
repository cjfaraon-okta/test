apiVersion: batch/v1
kind: Job
metadata:
  name: before-0
  namespace: default
spec:
  template:
    spec:
      containers:
        - name: sleep
          image: alpine:latest
          command: ["sleep", "20"]
#          command: [ "false" ]
      restartPolicy: Never
  backoffLimit: 0